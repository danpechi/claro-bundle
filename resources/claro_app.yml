resources:
  apps:
    claro_app:
      name: "claro-monitor-${var.schema}"
      description: "Claro Perú — Quality Monitor · Audio Transcription · Emotion Analysis"
      source_code_path: ..
      # Env vars are configured in app.yaml in the source directory.
